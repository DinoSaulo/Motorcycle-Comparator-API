/** Deliberate test-only grouping: a security regression suite that cuts across every controller and filter instead of
 *  mirroring the {@code main} layout. Findings needing a {@code src/main} change live in {@code SECURITY-AUDIT.md}. */
package com.motorcycle.comparison.security;
